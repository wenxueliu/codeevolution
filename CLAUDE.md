# CLAUDE.md

> **CodeEvolution** — 代码仓功能演进分析 + 业务知识逆向系统。

本服务项目文档（项目概述 / 技术栈 / 命令 / 能力矩阵 / 打包重启 / 提交规则）已合并到 **AGENTS.md**（本仓库唯一文档源）。Claude Code 会自动加载同级 `AGENTS.md`，此处 `@` 引用仅为兼容只读 `CLAUDE.md` 的入口。

> @AGENTS.md
